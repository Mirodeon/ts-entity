import {ISerializable} from "../entity/serializable/ISerializable";
import {IPersistable} from "../entity/persistable/IPersistable";

export interface IMetadata extends ISerializable, IPersistable {
}